import 'package:flutter/material.dart';

class Timings {
  const Timings._();

  static const Duration longPressThreshold = Duration(milliseconds: 280);
  static const Duration appearanceLabExitFade = Duration(milliseconds: 400);
  static const Duration appearanceLabBlend = Duration(milliseconds: 250);
  static const Duration councilPause = Duration(seconds: 1);
  static const Duration councilSigilRevealDelay = Duration(milliseconds: 300);
  static const Duration councilSigilFade = Duration(milliseconds: 500);
  static const Duration accentFadeOut = Duration(milliseconds: 400);
  static const Duration overlayHintDuration = Duration(seconds: 4);
  static const Duration skinCrossfade = Duration(milliseconds: 500);
}

