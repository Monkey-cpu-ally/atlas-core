import '../../domain/models/ui_prefs.dart';

class Defaults {
  const Defaults._();

  static const String defaultSkinId = 'lumen_core';
  static const String defaultMode = 'blueprint';
  static const UiPrefs uiPrefs = UiPrefs();
}

